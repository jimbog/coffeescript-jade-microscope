template(name='postItem')
    .post
        .post-content
            h3
                a(href='{{url}}') {{title}} <span> {{domain}} </span>
            p submitted by {{author}}
                if ownPost
                    a(href="{{pathFor 'postEdit'}}") Edit
        a(href="{{pathFor 'postPage'}}" class="discuss btn") Discuss